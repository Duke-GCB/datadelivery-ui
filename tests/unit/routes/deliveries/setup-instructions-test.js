import { moduleFor, test } from 'ember-qunit';

moduleFor('route:deliveries/setup-instructions', 'Unit | Route | deliveries/setup instructions', {
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
