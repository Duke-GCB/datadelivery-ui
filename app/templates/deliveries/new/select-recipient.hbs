{{#select-recipient-page projectName=project.name
                         backRoute=backRoute
                         disableBack=true
                         nextRoute=nextRoute
                         disableNext=disableNext }}
  {{duke-ds-user-list recipients pageSize=12 selectionChanged=(action 'toUserSelectionChanged')}}
{{/select-recipient-page}}
{{outlet}}
