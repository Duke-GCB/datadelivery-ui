{{#each crumbs as |crumb|}}
  {{breadcrumb-item label=crumb.label
                    routeName=crumb.routeName
                    selectedRouteName=strippedRouteName}}
{{/each}}
{{user-info user=user}}
